#include "pch.h"
#include "Collider.h"


Collider::Collider() :
	: body(body)
{
}


Collider::~Collider()
{
}

bool Collider::checkCollision(Collider & other, float push) 
{
	sf::Vector2f otherPosition = other.GetPosition();
	sf::Vector2f otherPosition = other.GetPosition();
	sf::Vector2f otherPosition = other.GetPosition();
	sf::Vector2f otherPosition = other.GetPosition();
	return false;
}
